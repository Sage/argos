Ext.data.JsonP.v2_beyond_the_guide_detail_layout_properties({"guide":"<h1>Detail Layout Properties</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-1'>Provider</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-2'>Value</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-3'>OnCreate</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-4'>Renderer</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-5'>Template</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-6'>Use</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-7'>Encode</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_detail_layout_properties-section-8'>Cls</a></li>\n</ol>\n</div>\n\n<p>Each row object has many properties available that allow you to control almost every aspect of the row. For Detail View rows these properties are:</p>\n\n<ul>\n<li> Provider</li>\n<li> Value</li>\n<li> onCreate</li>\n<li> Renderer</li>\n<li> Template</li>\n<li> Encode</li>\n<li> Cls</li>\n<li> Use</li>\n</ul>\n\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-1'>Provider</h2>\n\n<p>The <code>provider</code> property allows you to provide a custom mapping between the row and the SData entry. Normally it is a one to one mapping\nof the rows <code>property</code> value to the entry property e.g.:</p>\n\n<pre><code>// row\n{\n   name: 'NameLF',\n   property: 'NameLF',\n   label: this.nameLFText,\n}\n\n// SData Entry\n{\n   Account: 'Tests Inc.',\n   NameLF: 'McTest, Tester',\n   Email: 'tester@tests.org'\n}\n\n// Resulting row value:\nrow.value = entry[row.property]\nrow.value = entry['NameLF']\nrow.value = 'McTest, Tester'\n</code></pre>\n\n<p>If the one-to-one mapping is not desired you may define a <code>provider</code> function that takes the SData entry and the <code>property</code> of the row and\nwhatever it returns will be the value of the row.</p>\n\n<p>1. If you've followed the Argos-Template Guide, open <code>argos-template/src/Views/Account/Detail.js</code> and go to the <code>createLayout()</code> function.</p>\n\n<p>2. Add two new fields to the querySelect array: Type and SubType</p>\n\n<pre><code>querySelect: [\n  'AccountName',\n  'AccountManager/UserInfo/Username',\n  'WebAddress',\n  'MainPhone',\n  'Industry',\n  'Type',\n  'SubType'\n]\n</code></pre>\n\n<p>3. Add a new row to Details section children array with the following properties:</p>\n\n<pre><code>{\n    name: 'CombinedType',\n    property: 'CombinedType',\n    label: this.combinedTypeText,\n    provider: this.combineTypes\n}\n</code></pre>\n\n<p>4. Wait! We added a property that does not correspond to our data (querySelect), this is because when you provide a <code>provider()</code> function\nit will pass the SData entry to your function and let you figure out how to extract the property <code>CombinedType</code>.</p>\n\n<p>5. Define a new function (above createLayout) named <code>combineTypes</code>, for parameters you have <code>entry</code> and <code>property</code>. It should return\na string that joins <code>entry['Type']</code> and <code>entry['SubType']</code> with <code>&gt;</code>.</p>\n\n<pre><code>combineTypes: function(entry, property) {\n   if (entry['Type'] &amp;&amp; entry['SubType'])\n       return string.substitute('${Type} &gt; ${SubType}', entry);\n   else\n       return entry['Type'] || '';\n},\n</code></pre>\n\n<p>6. Add the localizable label string:</p>\n\n<pre><code>combinedTypeText: 'type',\n</code></pre>\n\n<p>7. Save and run your up and open an Detail for an entry. At type you should see something like: <code>Customer &gt; Hardware</code>.</p>\n\n<p>That's it for <code>provider</code> - you may use it to combine values, do different lookups based on custom logic or anything you like for pulling a\nvalue out of the SData entry.</p>\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-2'>Value</h2>\n\n<p>Setting the <code>value</code> property of a row object makes it skip the SData mapping and let's you set a value directly. While the use case for\n static values in a data-driven app is limited it is typically used to set a placeholder while another request is made for the real data.</p>\n\n<p>We will now add a loading text value directly and then in the <code>onCreate</code> topic you can see it expanded to be replaced with an asych call.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/Detail.js</code> and go to the <code>createLayout()</code> function.</p>\n\n<p>2. Add to querySelect the field <code>Address/$key</code>.</p>\n\n<pre><code>querySelect: [\n  'AccountName',\n  'AccountManager/UserInfo/Username',\n  'WebAddress',\n  'MainPhone',\n  'Industry',\n  'Type',\n  'SubType',\n  'Address/$key'\n]\n</code></pre>\n\n<p>3. Create a new row object under the Contact Info section and use \"Address\", \"Address/$key\", <code>this.addressText</code> and\n <code>this.loadingText</code> for name, property, label and value respectively.</p>\n\n<pre><code>{\n    name: 'Address',\n    property: 'Address/$key',\n    label: this.addressText,\n    value: this.loadingText\n}\n</code></pre>\n\n<p>4. Add the localizable strings to the view</p>\n\n<pre><code>addressText: 'address',\nloadngText: 'loading...'\n</code></pre>\n\n<p>5. Save, run your app and go to a Detail view entry. The value should always show the loading text, no matter what entry\nyou go to. As mentioned before this is usually tied to the <code>onCreate</code> property for retrieving a different SData entry and setting the result.</p>\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-3'>OnCreate</h2>\n\n<p>Every time a Detail View is <code>refresh()</code>ed (when it gets new data) it deletes the entire layout and re-constructs it with the new data.\nThis is in contrast to Edit Views where on <code>refresh()</code> they merely empty out the inputs. When the layout is being constructed it goes\nrow by row and when a row is added to the DOM it fires the rows <code>onCreate</code> function and passes all sorts of data.</p>\n\n<p>This exercise is a follow up to the <code>value</code> section above where the static loading text was set as a placeholder, we will now add\na <code>onCreate</code> function that gets new data and replaces the placeholder.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/Detail.js</code> and go to the <code>createLayout()</code> function.</p>\n\n<p>2. In the <code>Address</code> row add the key <code>onCreate</code> with a value of <code>this.requestAddress</code>:</p>\n\n<pre><code>{\n    name: 'Address',\n    property: 'Address/$key',\n    label: this.addressText,\n    value: this.loadingText,\n    onCreate: this.requestAddress\n}\n</code></pre>\n\n<p>3. Add a new function named requestAddress and its parameters are: row, node, value and entry:</p>\n\n<pre><code>requestAddress: function(row, node, value, entry) {\n    // row refers to the row object (where we just added onCreate)\n    // node is the corresponding HTML Node in the DOM for this row\n    // value is set to loading... so not much of a concern\n    // entry is the full SData entry response\n\n},\n</code></pre>\n\n<p>4. While out of scope for going into the details of making SData requests you can just follow along. If you want to know more about\nthe functions used here please see the github repo <a href=\"https://github.com/Sage/SDataJavaScriptClientLib\">SDataJavascriptClientLib</a>. Add the SData request:</p>\n\n<pre><code>requestAddress: function(row, node, value, entry) {\n   var request = this.createRequest();\n   request.setResourceKind('addresses');\n   request.setResourceSelector(entry['Address']['$key']);\n   request.setQueryArg(Sage.SData.Client.SDataUri.QueryArgNames.Select, 'FullAddress');\n\n   request.read({\n      success: lang.hitch(this, this.onRequestAddressSuccess, row, node, entry),\n      failure: this.onRequestAddressFailure,\n      scope: this\n   });\n},\n</code></pre>\n\n<p>5. To briefly cover what is going on, we are using the views <code>createRequest()</code> function which sets up a single entry request but it is using\nour views resourceKind, querySelect, etc so we need to change those to be for <code>Address</code>. Then it executes (reads) the request passing\nthe callbacks and the scope of the callbacks. The <code>lang.hitch()</code> is used to \"append\" arguments to the <code>onRequestAddressSuccess</code> function.</p>\n\n<p>6. As we needed the dojo lang module, add that as a dependency and reference:</p>\n\n<pre><code>define('Mobile/Template/Views/Account/Detail', [\n    'dojo/_base/declare',\n    'dojo/_base/lang',\n    'dojo/string',\n    'Sage/Platform/Mobile/Detail'\n], function(\n    declare,\n    lang,\n    string,\n    Detail\n) {\n</code></pre>\n\n<p>7. Now define the onAddressRequestSuccess like so:</p>\n\n<pre><code>onAddressRequestSuccess: function(row, node, entry, response) {\n   var address = response['FullAddress'];\n   query('span', node).text(address);\n},\n</code></pre>\n\n<p>8. In the above code we extracted the value and then used <code>dojo.query</code> on the HTML Node we got from onCreate.\nIf you inspect the HTML of a row you will see that there are various parts and the text the user see's is within the\nonly child span element. Since we are now using the dojo query module add that as a dependency and reference:</p>\n\n<pre><code>define('Mobile/Template/Views/Account/Detail', [\n    'dojo/_base/declare',\n    'dojo/_base/lang',\n    'dojo/query',\n    'dojo/string',\n    'Sage/Platform/Mobile/Detail'\n], function(\n    declare,\n    lang,\n    query,\n    string,\n    Detail\n) {\n</code></pre>\n\n<p>9. If you now save and load your application and navigate to a Detail entry you should see the text <code>loading...</code> until the request is\nfired and returned. Then it will change to the Full Address property of the Address Key provided by Address/$key.</p>\n\n<p>While this example is a simple one, the <code>onCreate</code> attribute may be used for any purpose that fires when the row is created.</p>\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-4'>Renderer</h2>\n\n<p>The <code>renderer</code> property is used a \"presenter\" it takes the existing value and changes the rendered format without modifying the value underneath.\nSome examples are phone numbers, addresses, dates, clickable URLs, boolean to yes/no text, and others. Note that <code>renderer</code> merely alters the\nvalue into a different (or new) string.</p>\n\n<p>This exercise assumes you've completed Argos-Template Guide.</p>\n\n<p>Renderer may also be set a function of your view, let's take the Industry row and have a custom message when a name is matched.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/Detail.js</code> and go to the <code>createLayout()</code> function.</p>\n\n<p>2. In the Industry row object add the key <code>renderer</code> set to <code>this.formatIndustry</code>:</p>\n\n<pre><code>          },{\n              name: 'Industry',\n              property: 'Industry',\n              label: this.industryText,\n              renderer: this.formatIndustry\n          },{\n</code></pre>\n\n<p>3. Create the function <code>formatIndustry()</code> in your view and it is being passed a single parameter: <code>value</code></p>\n\n<pre><code>formatIndustry: function(value) {\n   // needs to return a formatted value\n},\n</code></pre>\n\n<p>4. Add a new property to your value (up near your localizable strings) named: industryDescriptionText and have it be a object with a\nkey of <code>Communications</code> and value of <code>Communications including phone, fax and e-mail</code>:</p>\n\n<pre><code>industryDescriptionText: {\n    'Transportation/Shipping': 'Shipping including ground, air and boat'\n},\n</code></pre>\n\n<p>5. Back in the formatIndustry have it check for the value within our industryDescription object and send the description value back, if it is\nnot there send back just the industry value:</p>\n\n<pre><code>formatIndustry: function(value) {\n   return this.industryDescriptionText[value] || value;\n},\n</code></pre>\n\n<p>6. Save and run your app and view the Account entry \"Allied Corp.\" which happens to have the Trans/Shipping industry. You should now\nsee the custom message instead of the value. For other entries it should still show the industry.</p>\n\n<p>Renderer is also commonly used to take Object values and return the displayed format - for example if you set a field to <code>owner/*</code> you could use renderer\nto return <code>value.FirstName + value.LastName</code>. The implementations are endless when it comes to renderer.</p>\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-5'>Template</h2>\n\n<p>The <code>template</code> attribute, or its shorthand <code>tpl</code>, provides a Simplate override for this row, but only to the value part. Meaning the row will still be\na <code>&lt;div&gt;</code> with a <code>&lt;label&gt;</code> and <code>&lt;span&gt;</code> children with the resulting markup of template being put into the <code>&lt;span&gt;</code>. If you want to change the\nentire row markup see <code>use</code>.</p>\n\n<p>This exercise assumes you've complete Argos-Template Guide, which already covered using a template for setting the first and last name of the owner.</p>\n\n<p>Now we will alter the markup to include an icon of the business if we have one defined.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/Detail.js</code> and go to the <code>createLayout()</code> function.</p>\n\n<p>2. In the 'AccountName' row add the <code>template</code> attribute and set it as follows:</p>\n\n<pre><code>          },{\n              name: 'AccountName',\n              property: 'AccountName',\n              label: this.accountNameText,\n              template: new Simplate([\n                 '{% if ($$.logos[$.value]) { %}',\n                  '&lt;img src=\"$$.logos[$.value]\"&gt;',\n                  '{% } %}',\n                  '{%= $.value %}'\n              ])\n          },{\n</code></pre>\n\n<p>3. Define the <code>logos</code> hash on the view itself (up near id, editView):</p>\n\n<pre><code>logos: {\n    'Abbott Ltd.': 'content/images/icons/Company_24.png'\n}\n</code></pre>\n\n<p>4. Save and reload. If you view the <code>Abbott Ltd.</code> entry the account name should now also include an image of a company, other accounts should just show the name.</p>\n\n<p>While it may seem that Renderer and Template overlap, it is more the intent of what you want to accomplish. If you want to simply alter a value or change\nhow a value is displayed - use <code>renderer</code>. If you want to wrap the value in additional HTML then use <code>template</code>. The last case - you want to alter the entire\nrows HTML markup you need to use <code>use</code>.</p>\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-6'>Use</h2>\n\n<p>While <code>template</code> is useful for modifying the value, <code>use</code> modifies the markup of the entire row. It gets passed the row object after all the\nother modifications have taken place.</p>\n\n<p>First we'll take a look at the standard template for rows and then add a new template that includes an icon before label.</p>\n\n<p>1. Open <code>argos-sdk/src/Detail.js</code> and find the <code>propertyTemplate</code> property and you will see this:</p>\n\n<pre><code>    propertyTemplate: new Simplate([\n        '&lt;div class=\"row {%= $.cls %}\" data-property=\"{%= $.property || $.name %}\"&gt;',\n        '&lt;label&gt;{%: $.label %}&lt;/label&gt;',\n        '&lt;span&gt;{%= $.value %}&lt;/span&gt;',\n        '&lt;/div&gt;'\n    ]),\n</code></pre>\n\n<p>2. As you can see the default one already uses the basic properties directly but just so you aware any property defined on your row object is\navailable via <code>$</code> and the detail view via <code>$$</code>.</p>\n\n<p>3. Now go back to your <code>argos-template/src/Views/Account/Detail.js</code> and under <code>createLayout()</code> find the Industry row we changed earlier to\ninclude a message.</p>\n\n<p>4. Add the key <code>use</code> and set it to <code>this.rowWithIconTemplate</code>:</p>\n\n<pre><code>          },{\n              name: 'Industry',\n              property: 'Industry',\n              label: this.industryText,\n              renderer: this.formatIndustry,\n              use: this.rowWithIconTemplate\n          },{\n</code></pre>\n\n<p>5. On the view itself define a new property named <code>rowWithIconTemplate</code> and have it define a new Simplate with the following:</p>\n\n<pre><code>rowWithIconTemplate: new Simplate([\n    '&lt;div class=\"row {%= $.cls %}\" data-property=\"{%= $.property || $.name %}\"&gt;',\n        '&lt;img style=\"position:absolute\" src=\"{%= $$.industryIcons[$.Industry] || $$.industryIcons[\"default\"] %}\"&gt;',\n        '&lt;label\"&gt;{%: $.label %}&lt;/label&gt;',\n        '&lt;span&gt;{%= $.value %}&lt;/span&gt;',\n    '&lt;/div&gt;'\n]),\n</code></pre>\n\n<p>6. Then define on the view itself the <code>industryIcons</code> object that maps the industry values to their icons;</p>\n\n<pre><code>industryIcons: {\n   'Communications': 'content/images/icons/Call_24x24.png',\n   'default': 'content/images/icons/Company_24.png'\n},\n</code></pre>\n\n<p>7. To walk through all that we defined:</p>\n\n<p>   a) When the detail view is passed a new key, requests data and starts to process the results</p>\n\n<p>   b) The layout gets recreated and loops through the entire <code>this.layout</code> array and builds each row</p>\n\n<p>   c) When it gets to Industry it see's we defined a <code>use</code> template and calls it passing the row object itself and the view for scope</p>\n\n<p>   d) In the Simplate we have it using pretty much the same markup except for an <code>&lt;img&gt;</code> element where the source is set using the object map <code>industryIcons</code></p>\n\n<p>   e) The resulting HTML is then added to the DOM for the Industry row and it continues on.</p>\n\n<p>8. Save and run your app you should now see a little icon to the left of label for industry rows. As you can see you can easily override the\ndefault <code>propertyTemplate</code> via the <code>use</code> property.</p>\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-7'>Encode</h2>\n\n<p>Encode is a flag that runs the value through <code>argos-sdk/src/Format</code>'s encode function that encodes <code>&amp;&gt;&lt;\"</code> to their HTML entities. By default it\nis <code>true</code> - all values get encoded, however if you are using a <code>renderer</code> or <code>template</code> then the default is <code>false</code> and must be\nexplicitly set to <code>true</code> in the row object to be encoded.</p>\n\n<p>This exercise will turn encoding off so you can see the effects.</p>\n\n<p>1. Open the <code>argos-template</code> app and go to the Account List view. Do a search for \"space\" and select <code>Spacetek &amp; Correct</code>.</p>\n\n<p>2. In the Detail view notice that the AccountName row correctly shows <code>Spacetek &amp; Correct</code>.</p>\n\n<p>3. Now open up your <code>argos-template/src/Views/Account/Detail.js</code> and down to <code>createLayout()</code> and the AccountName row.</p>\n\n<p>4. Add the key <code>encode</code> and set it to false:</p>\n\n<pre><code>          children: [{\n              name: 'AccountName',\n              property: 'AccountName',\n              label: this.accountNameText,\n              encode: false\n          },{\n</code></pre>\n\n<p>5. Save and run (doing a search again) and go to the Detail view. Now the AccountName row shows <code>Spacetek &amp;amp; Correct</code>\n(note: most modern browsers will auto-escape for you and you may need a debugger in the format encode function to see it did alter the value).</p>\n\n<p>6. Go back and remove the <code>encode: false</code> - this was just to demonstrate - and save.</p>\n\n<h2 id='v2_beyond_the_guide_detail_layout_properties-section-8'>Cls</h2>\n\n<p>Cls is a quick way to add a custom CSS class to the container div of the row markup.</p>\n\n<p>This can be used for many purposes but the only one provided out of the box is <code>content-loading</code> and we will see that in action.</p>\n\n<p>1. Assuming the you've completed the <code>value</code> example and the followup onCreate exercises, open your <code>Account/Detail.js</code> and down\nto <code>createLayout</code> and the <code>Address</code> row object we added with the placeholder that gets replaced.</p>\n\n<p>2. Add the key <code>cls</code> and set to <code>'content-loading'</code>:</p>\n\n<pre><code>{\n    name: 'Address',\n    property: 'Address/$key',\n    label: this.addressText,\n    value: this.loadingText,\n    onCreate: this.requestAddress,\n    cls: 'content-loading'\n}\n</code></pre>\n\n<p>3. If you want to view the corresponding CSS open <code>argos-sdk/content/css/base.css</code> and you'll find the following definition:</p>\n\n<pre><code>.content-loading &gt; span\n{\n    padding-left: 32px;\n    font-size: 0.7em;\n    background: url('../images/content-loading-light-green.gif') no-repeat scroll 12px 50%;\n}\n</code></pre>\n\n<p>4. Save, and open a detail view. You'll now see a loading indicator in the field itself next to the <code>loading...</code> text.\nYet when the data is loaded the text changes but the loading indicator is still there.\nTo fix that we edit the success handler to remove the class:</p>\n\n<pre><code>onAddressRequestSuccess: function(row, node, entry, response) {\n   var address = response['FullAddress'];\n   query('span', node).text(address);\n   domClass.remove(node, 'content-loading');\n},\n</code></pre>\n\n<p>5. Since we now need the dojo dom-class module go ahead and add that as a dependency/reference:</p>\n\n<pre><code>define('Mobile/Template/Views/Account/Detail', [\n    'dojo/_base/declare',\n    'dojo/_base/lang',\n    'dojo/dom-class',\n    'dojo/query',\n    'dojo/string',\n    'Sage/Platform/Mobile/Detail'\n], function(\n    declare,\n    lang,\n    domClass,\n    query,\n    string,\n    Detail\n) {\n</code></pre>\n\n<p>6. Save and run - now when you go to a Detail View you get a loading indicator while the field request is loading and on success it puts\nthe new text and removes the indicator.</p>\n\n<p>Cls is used to provide quick one off css adjustments, if you need to greatly change the markup and styling see the <code>use</code> attribute for how to do that.</p>\n","title":"Detail Layout Properties"});