Ext.data.JsonP.v2_appendix_wire_list_to_edit({"guide":"<h1>Wiring List to Edit View</h1>\n\n<p>Once you have an Edit View defined for an entity you will need to go back to the Detail and List view to point them in the right direction. This topic covers going from List to Edit.</p>\n\n<p>1. List views have the <code>insertView</code> property and that should point to the id of the view that will handle inserting entities of the same kind.</p>\n\n<p>2. Since the Insert button is a toolbar item it is defined in the <code>createToolLayout</code> function. Open up <code>argos-sdk/src/List.js</code> and go to it.</p>\n\n<p>3. Only one item is defined and its action is <code>navigateToInsertView</code>:</p>\n\n<pre><code>            'tbar': [{\n                id: 'new',\n                action: 'navigateToInsertView',\n                security: <a href=\"#!/api/Sage.Platform.Mobile.Application-method-getViewSecurity\" rel=\"Sage.Platform.Mobile.Application-method-getViewSecurity\" class=\"docClass\">App.getViewSecurity</a>(this.insertView, 'insert')\n            }]\n</code></pre>\n\n<p>4. Scroll down to the <code>navigateToInsertView()</code> function and we have this:</p>\n\n<pre><code>        navigateToInsertView: function(el) {\n            var view = <a href=\"#!/api/Sage.Platform.Mobile.Application-method-getView\" rel=\"Sage.Platform.Mobile.Application-method-getView\" class=\"docClass\">App.getView</a>(this.insertView || this.editView);\n            if (view)\n            {\n                view.show({\n                    returnTo: this.id,\n                    insert: true\n                });\n            }\n        },\n</code></pre>\n\n<p>5. The important part here is that it pulls the <code>insertView</code> property we fixed and that in the navigation options it is passing <code>{insert: true}</code>. To tell an Edit view that we are inserting a new entity we must pass that in the navigation options, many of the Edit view functions check against that value for determining to do a \"INSERT\" or \"UPDATE\" command, to request a $template for default values, if values are \"dirty\" by default and others.</p>\n\n<p>6. When the user presses the Save toolbar item in the Edit View it will perform an PUT (insert), collecting all the values and sending them as a new entity. On success a global event is published and the List view will be refreshed when returning to it.</p>\n","title":"Wiring List to Edit"});