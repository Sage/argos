Ext.data.JsonP.v2_template_guide({"guide":"<h1>Argos-Template</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/v2_template_guide-section-1'>Setup</a></li>\n<li><a href='#!/guide/v2_template_guide-section-2'>Configuring an SData Connection</a></li>\n<li><a href='#!/guide/v2_template_guide-section-3'>Create a Simple List View</a></li>\n<li><a href='#!/guide/v2_template_guide-section-4'>Adding a List View to the Home Page</a></li>\n<li><a href='#!/guide/v2_template_guide-section-5'>Create a Simple Detail View</a></li>\n<li><a href='#!/guide/v2_template_guide-section-6'>Detail Layouts: Renderer and Template</a></li>\n<li><a href='#!/guide/v2_template_guide-section-7'>Create a Simple Edit View</a></li>\n<li><a href='#!/guide/v2_template_guide-section-8'>Edit View Layouts</a></li>\n</ol>\n</div>\n\n<p>By the end of this guide you will have an enterprise data-driven mobile application with the following:</p>\n\n<ul>\n<li>A Home Navigation page;</li>\n<li>Two List, or Collection, Views bound to different entities;</li>\n<li>A Detail View showing in depth properties, a single quick action and a link to a related List View; and</li>\n<li>A dual-purpse Edit View used to create and update entities.</li>\n</ul>\n\n\n<p>As you are following along the guide will cover topics such as: Configuring SData, AMD loading and Simplates. From start to finish the guide should take around 15-20 minutes to complete and there are many more advanced topics to address specific implementations.</p>\n\n<h2 id='v2_template_guide-section-1'>Setup</h2>\n\n<p>The Argos framework is meant to serve in an environment of multiple on-going applications and their modules, as such the folder structure allows a single copy of Argos-SDK. You may opt for a different structure but the following provides an extensible foundation.</p>\n\n<p>1. Create a folder called <code>mobile</code> and within that, create a folder called <code>argos-sdk</code> and a folder called <code>products</code>.</p>\n\n<p>2. You should now have <code>\\mobile</code>, <code>\\mobile\\argos-sdk</code> and <code>\\mobile\\products</code>.</p>\n\n<p><p><img src=\"guides/v2_template_guide/setup_folders.png\" alt=\"Folder Structure\"></p></p>\n\n<p>3. All applications will be a new folder within <code>products</code>.</p>\n\n<p>4. Download the SDK and extract the zip into the <code>argos-</code>sdk` folder: <a href=\"https://github.com/Sage/argos-sdk/downloads\">https://github.com/Sage/argos-sdk/downloads</a></p>\n\n<p>5. A bare-bones app that this guide will use as a kickstarter has been created as Argos-Template. Download it from here: <a href=\"https://github.com/Sage/argos-template/downloads\">https://github.com/Sage/argos-template/downloads</a></p>\n\n<p>6. Create a new folder under <code>products</code> named <code>argos-template</code> and place the extracted contents there.</p>\n\n<p>7. Within the <code>argos-template</code> folder open <code>index-dev.html</code>.</p>\n\n<p><p><img src=\"guides/v2_template_guide/intro_home.png\" alt=\"Folder Structure\"></p></p>\n\n<p>Congratulations you have your first Argos app!</p>\n\n<p>If you want to know about all the files you downloaded, see <a href=\"#!/guides/v2_overview\">Overview</a> - for an explanation of all files and folders currently in <code>argos-template</code>.</p>\n\n<h4>Local Files</h4>\n\n<p>Running <code>index-dev.html</code> off the local file system may not always work as files loaded in this way is treated as a security concern in certain browsers. You should setup a local server (ISS, WAMP, XAMPP, etc) and run from localhost.</p>\n\n<h4>Cache</h4>\n\n<p>Be aware that modern browsers cache things heavily and you should make sure that when opening or refreshing your app it is always the latest code. Many use \"private\" browsing features to disable cookies and cache. If using a local server make sure it is also set to not cache any files.</p>\n\n<h2 id='v2_template_guide-section-2'>Configuring an SData Connection</h2>\n\n<p>SData is the lifeblood of Argos-SDK, all data input and output are bound to the protocol and the base List, Detail and Edit Views process and construct the data bound calls in SData format.</p>\n\n<p>To set up the service that all calls will use we will point to a public provider with sample data and test user.</p>\n\n<p>1. Open <code>argos-template/configuration/development.js</code></p>\n\n<pre><code>define('configuration/development', ['Mobile/Template/ApplicationModule'], function(ApplicationModule) {\n\n    return {\n        modules: [\n            new ApplicationModule()\n        ],\n        connections: {\n            'crm': {\n                isDefault: true,\n                offline: true,\n                url: '',\n                json: true\n            }\n        }\n    };\n\n});\n</code></pre>\n\n<p>2. The configuration should return an object containing the <code>connections</code> key. That is where all SData connections will be defined.</p>\n\n<p>3. Within the <code>connections</code> object, each key will define a service. Currently there is a <code>crm</code> service with an empty url, edit it to point to our sample provider:</p>\n\n<pre><code>http://50.16.242.109/sdata/slx/dynamic/-/\n</code></pre>\n\n<p>4. Now add two new keys: username and password and set them to <code>'lee'</code> and <code>''</code> (blank) respectively:</p>\n\n<pre><code>    connections: {\n        'crm': {\n            isDefault: true,\n            offline: true,\n            userName: 'lee',\n            password: '',\n            url: 'http://50.16.242.109/sdata/slx/dynamic/-/',\n            json: true\n        }\n    }\n</code></pre>\n\n<p>5. SData is fully configured and we are now ready to start creating views that retrieve data.</p>\n\n<h2 id='v2_template_guide-section-3'>Create a Simple List View</h2>\n\n<h3>List Views</h3>\n\n<p>List Views are all about presenting large amounts of data in an easy to read list format. Generally each list item will take you to a Detail View for that item.</p>\n\n<p>When making any new Views you need to do two things:</p>\n\n<p>1. Code the view; and</p>\n\n<p>2. Register the view in <code>ApplicationModule.js</code>.</p>\n\n<h3>Step 1: Coding a List View</h3>\n\n<p>List Views are designed to display a collection or list of a single entity. Some examples include: employees, artists, bug reports or sales orders.</p>\n\n<p>For our entity choice will be making a List (and Detail, Edit) View of the entity \"Account\" which uses the SData endpoint \"accounts\".</p>\n\n<p>1. Create a new folder in <code>argos-template\\src\\Views</code> named \"Account\".</p>\n\n<p>2. Create an empty file named <code>List.js</code> into <code>argos-template\\src\\Views\\Account\\</code> and open it up.</p>\n\n<p>3. First we setup the declare and define according the AMD standard. For more information see <a href=\"#!/guides/v2_appendix_amd\">Appendix: AMD</a>.</p>\n\n<pre><code>define('Mobile/Template/Views/Account/List', [\n    'dojo/_base/declare',\n    'dojo/string',\n    'Sage/Platform/Mobile/List'\n], function(\n    declare,\n    string,\n    List\n) {\n    return declare('Mobile.Template.Views.Account.List', [List], {\n    });\n});\n</code></pre>\n\n<p>4. All views have a <code>titleText</code> and <code>id</code> property so let's define those first. The id standard for list is \"entity_list\" - so ours is <code>account_list</code>.</p>\n\n<pre><code>    return declare('Mobile.Template.Views.Account.List', [List], {\n        // Localization\n        titleText: 'Account List',\n\n        // View Properties\n        id: 'account_list'\n    });\n</code></pre>\n\n<p>5. Add the <code>icon</code> property and it set to <code>'content/images/icons/Company_24.png'</code>. The <code>icon</code> will be used by our <code>Home</code> View.</p>\n\n<pre><code>    return declare('Mobile.Template.Views.Account.List', [List], {\n        // Localization\n        titleText: 'Account List',\n\n        // View Properties\n        id: 'account_list',\n        icon: 'content/images/icons/Company_24.png'\n    });\n</code></pre>\n\n<p>6. Now for the data. The List view was several properties that control what data is retrieved: resourceKind, querySelect and queryOrderBy.</p>\n\n<ul>\n<li>resourceKind to <code>'accounts'</code>. Entity/endpoint name.</li>\n<li>querySelect to an array of strings for: <code>AccountName</code>, <code>AccountManager/UserInfo/UserName</code>. Fields of the entity that will be returned.</li>\n<li><p>queryOrderBy to <code>'AccountName asc'</code>. Server-side ordering.</p>\n\n<pre><code>  return declare('Mobile.Template.Views.Account.List', [List], {\n      // Localization\n      titleText: 'Account List',\n\n      // View Properties\n      id: 'account_list',\n      icon: 'content/images/icons/Company_24.png',\n      resourceKind: 'accounts',\n      querySelect: [\n          'AccountName',\n          'AccountManager/UserInfo/Username'\n      ],\n      queryOrderBy: 'AccountName asc'\n  });\n</code></pre></li>\n</ul>\n\n\n<p>7. Every row is constructed by a List Views <code>itemTemplate</code> property. All templates use Simplate, which is a templating engine where the item entry and the list view is passed as variables: <code>$</code> and <code>$$</code>. See <a href=\"#!/api/Simplate\" rel=\"Simplate\" class=\"docClass\">Simplate</a> for more information on the template engine.</p>\n\n<p>8. Add the following itemTemplate to your view:</p>\n\n<pre><code>        // Templates\n        itemTemplate: new Simplate([\n            '&lt;h3&gt;{%: $.AccountName %}&lt;/h3&gt;',\n            '&lt;h4&gt;{%: $.AccountManager &amp;&amp; $.AccountManager.UserInfo ? $.AccountManager.UserInfo.UserName : \"\" %}&lt;/h4&gt;'\n        ])\n</code></pre>\n\n<p>11. Save your List view file, before it will be loaded in the app we need to register it in ApplicationModule.</p>\n\n<h3>Step 2: Register the View</h3>\n\n<p>Before any view or module is loaded into the application (via AMD) it needs to be:</p>\n\n<ul>\n<li>added as a dependency;</li>\n<li>verify the reference is passed in; and</li>\n<li>registered via the <code><a href=\"#!/api/Sage.Platform.Mobile.ApplicationModule-method-registerView\" rel=\"Sage.Platform.Mobile.ApplicationModule-method-registerView\" class=\"docClass\">ApplicationModule.registerView</a>()</code> function.</li>\n</ul>\n\n\n<p>1. Open <code>argos-template/src/ApplicationModule.js</code> and edit the define function to pull in our new List view:</p>\n\n<pre><code>define('Mobile/Template/ApplicationModule', [\n    /* trimmed for example */\n    'Mobile/Template/Views/Account/List',\n], function(\n    /* trimmed for example */\n    AccountList\n) {\n</code></pre>\n\n<p>2. Go to the <code>loadViews()</code> function, as you can see it is already registering the Home page. Following the same setup go ahead and register your list view using the referenced variable:</p>\n\n<pre><code>            this.registerView(new AccountList());\n</code></pre>\n\n<p>3. Save the file.</p>\n\n<p>Your view is now defined and registered and will be created, initialized and added to the DOM when your application starts.</p>\n\n<h3>Step 3: Quick Peek</h3>\n\n<p>If you open <code>index-dev.html</code> right now you will see the blank Home view and no way to get to the List view. However, we can take a quick peek and directly go to the view - this trick is useful for developing and debugging views.</p>\n\n<p>1. Open <code>index-dev.html</code>.</p>\n\n<p>2. Open your javascript console. See <a href=\"#!/guides/v2_appendix_browser-setup\">Browsers Help</a> for the shortcut key for your chosen browser.</p>\n\n<p>3. Type in: <code><a href=\"#!/api/Sage.Platform.Mobile.Application-method-getView\" rel=\"Sage.Platform.Mobile.Application-method-getView\" class=\"docClass\">App.getView</a>('account_list').show()</code></p>\n\n<p>4. Press enter.</p>\n\n<p><p><img src=\"guides/v2_template_guide/list-trick.png\" alt=\"List Trick\"></p></p>\n\n<p>Without going into too much detail the <code>App</code> namespace is glue that holds everything together and the line above is used to show any view.</p>\n\n<p>Features seen in the Quick Peek:</p>\n\n<ul>\n<li>View transition effect (slide) into place;</li>\n<li>Title bar with Back, Home and Add buttons;</li>\n<li>Search widget;</li>\n<li>Automatic paging (scroll down in the list to see Retrieve More);</li>\n<li>Navigation context and history (note the URL);</li>\n<li>Offline cache (inspect your local storage);</li>\n<li>Footer bar with Top and Copyright text; and</li>\n<li>Retrieved and rendered out 20 acccounts.</li>\n</ul>\n\n\n<p>The above is the default behaviour - every piece is customizable so that you can tailor it to your business needs.</p>\n\n<h2 id='v2_template_guide-section-4'>Adding a List View to the Home Page</h2>\n\n<p>With a List view defined lets add a link to it on the Home page.</p>\n\n<p>1. Open <code>argos-template/src/Application.js</code> and find the the <code>getDefaultViews</code> function. This function returns a list of view ids that should be on the Home screen by default. Add your new view id to the list:</p>\n\n<pre><code>        getDefaultViews: function() {\n            return [\n                'account_list'\n            ];\n        },\n</code></pre>\n\n<p>2. Save and open <code>argos-template/index-dev.html</code>.</p>\n\n<p><p><img src=\"guides/v2_template_guide/list-home.png\" alt=\"List on Home View\"></p></p>\n\n<p>The icon displayed came from the <code>icon</code> property defined on the list view.</p>\n\n<p>Clicking the row will take you to your Account List view where it will automatically construct an SData request, parse and present the results using <code>itemTemplate</code> and use a simple paging mechanism.</p>\n\n<h2 id='v2_template_guide-section-5'>Create a Simple Detail View</h2>\n\n<p>Detail Views are intended to display a wealth of knowledge all categorically organized with helpful actions and links to perform on this entry or take you to ones related.</p>\n\n<p>Creating a Detail View is very similar to creating a List in that you need to do three things:</p>\n\n<p>1. Code the Detail View;</p>\n\n<p>2. Register the view; and</p>\n\n<p>3. Wire the List View to the Detail View.</p>\n\n<h3>Step 1: Coding a Detail View</h3>\n\n<p>1. Create an empty file named <code>Detail.js</code> into <code>argos-template\\src\\Views\\Account\\</code> and open it up.</p>\n\n<p>2. First we setup the declare and define according the AMD standard. For more information see <a href=\"#!/guides/v2_appendix_amd\">Appendix: AMD</a>.</p>\n\n<pre><code>define('Mobile/Template/Views/Account/Detail', [\n    'dojo/_base/declare',\n    'dojo/string',\n    'Sage/Platform/Mobile/Detail'\n], function(\n    declare,\n    string,\n    Detail\n) {\n    return declare('Mobile.Template.Views.Account.Detail', [Detail], {\n    });\n});\n</code></pre>\n\n<p>3. All views have a <code>titleText</code> and <code>id</code> property so let's define those first. The id standard for detail is \"entity_detail\" making this one <code>account_detail</code>.</p>\n\n<pre><code>    return declare('Mobile.Template.Views.Account.Detail', [Detail], {\n        // Localization\n        titleText: 'Account Detail',\n\n        // View Properties\n        id: 'account_detail'\n    });\n</code></pre>\n\n<p>4. Now for the data. The two detail view properties we need are: resourceKind and querySelect. The resource kind is the endpoint of our entity and querySelect tells the request what fields we need.</p>\n\n<ul>\n<li>resourceKind to <code>'accounts'</code></li>\n<li><p>querySelect to an array of strings for: <code>AccountName</code>, <code>AccountManager/UserInfo/*</code>, <code>WebAddress</code>, <code>MainPhone</code>, and <code>Industry</code></p>\n\n<pre><code>  return declare('Mobile.Template.Views.Account.Detail', [Detail], {\n      // Localization\n      titleText: 'Account Detail',\n\n      // View Properties\n      id: 'account_detail',\n      resourceKind: 'accounts',\n      querySelect: [\n          'AccountName',\n          'AccountManager/UserInfo/*',\n          'WebAddress',\n          'MainPhone',\n          'Industry'\n      ]\n  });\n</code></pre></li>\n</ul>\n\n\n<p>5. All Detail views must define a <code>createLayout()</code> function that defines the view <code>layout</code> property. The property <code>layout</code> is an array of \"section\" objects. Each section has a property named \"children\" which in turn is an array of \"row\" objects.</p>\n\n<p>The sections get turn into collapsible headers with the children being the rows of information under that header.</p>\n\n<pre><code>// section example\n{\n    title: 'Section Header',\n    name: 'SectionUniqueId',\n    children: [/* rows */]\n}\n\n// row example\n{\n    name: 'UniqueRowId',\n    property: 'DataBoundProperty',\n    label: 'Label shown in user interface'\n}\n</code></pre>\n\n<p>6. The row objects <code>property</code> property should match up with your <code>querySelect</code>s, this ties the data from the SData response to the displayed row to the user. Further options for layout are covered in <a href=\"#!/guides/v2_beyond_the_guide_detail_layout_properties\">Advanced Detail Create Layout</a>.</p>\n\n<p>7. To utilize localization utilities of Argos all the row labels and section titles are defined as a property of the view and suffixed with <code>Text</code>. Any text string that is shown to the user should be defined this way.</p>\n\n<pre><code>        createLayout: function() {\n            return this.layout || (this.layout = [{\n                title: this.detailsText,\n                name: 'DetailsSection',\n                children: [{\n                    name: 'AccountName',\n                    property: 'AccountName',\n                    label: this.accountNameText\n                },{\n                    name: 'Industry',\n                    property: 'Industry',\n                    label: this.industryText\n                },{\n                    name: 'AccountManager',\n                    property: 'AccountManager.UserInfo',\n                    label: this.accountManagerText\n                }]\n           },{\n                title: this.contactInfoText,\n                name: 'ContactInfoSection',\n                children: [{\n                    name: 'WebAddress',\n                    property: 'WebAddress',\n                    label: this.webAddressText\n                },{\n                    name: 'MainPhone',\n                    property: 'MainPhone',\n                    label: this.mainPhoneText\n                }]\n            }]);\n        }\n</code></pre>\n\n<p>8. And add the localization strings for our labels/titles up near the titleText:</p>\n\n<pre><code>titleText: 'Account Detail',\naccountNameText: 'account',\nindustryText: 'business',\naccountManagerText: 'manager',\nwebAddressText: 'web',\nmainPhoneText: 'phone',\ndetailsText: 'Details',\ncontactInfoText: 'Contact Info',\n</code></pre>\n\n<p>9. That's it for a simple Detail View definition. Save your file.</p>\n\n<h3>Step 2: Register the View</h3>\n\n<p>As with all new views they must be registered in order to be loaded into the application.</p>\n\n<p>1. Open <code>argos-tempate/src/ApplicationModule.js</code> and add our new view as a dependency, making sure the reference is pulled in:</p>\n\n<pre><code>define('Mobile/Template/ApplicationModule', [\n    /* trimmed for example */\n    'Mobile/Template/Views/Account/List',\n    'Mobile/Template/Views/Account/Detail'\n], function(\n   /* trimmed for example */\n   AccountList,\n   AccountDetail\n) {\n</code></pre>\n\n<p>2. Under <code>loadViews()</code> register our view using the reference variable.</p>\n\n<pre><code>            this.registerView(new AccountList());\n            this.registerView(new AccountDetail());\n</code></pre>\n\n<p>3. Save the file.</p>\n\n<h3>Step 3: Wire List to Detail</h3>\n\n<p>Our Account List View does not know about the Account Detail View so let's add that relationship.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/List.js</code>.</p>\n\n<p>2. Near <code>id</code>, add the property <code>detailView</code> and set it to our detail id: <code>account_detail</code>:</p>\n\n<pre><code>    id: 'account_list',\n    detailView: 'account_detail',\n</code></pre>\n\n<p>3. Save and open your <code>index-dev.html</code> file.</p>\n\n<p>4. Navigate to Accounts, then select an Account to see your detail view</p>\n\n<p><p><img src=\"guides/v2_template_guide/detail-view.png\" alt=\"Detail View\"></p></p>\n\n<p>(Details may vary)</p>\n\n<p>For further details on how the wiring works please see <a href=\"#!/guides/v2_appendix_wire_list_to_detail\">Wiring List to Detail</a>.</p>\n\n<p>As you can see several of the fields don't look as they should, the phone number isn't formatted, the website isn't a hyperlink and the name just says Object. These rows require some additional properties in order to render correctly.</p>\n\n<h2 id='v2_template_guide-section-6'>Detail Layouts: Renderer and Template</h2>\n\n<p>Each row in a detail layout has the <code>renderer</code> property available which is used a \"presenter\". It takes the existing value and changes the rendered format without modifying the value underneath. Some examples are addresses, dates, clickable URLs, boolean to yes/no text, and others.</p>\n\n<p>Currently on our Detail View we have: an unclickable url and a name as <code>[Object object]</code>.</p>\n\n<h3>Step 1: URLs</h3>\n\n<p>Argos-SDK provides a number of built-in formatters available via <code><a href=\"#!/api/Sage.Platform.Mobile.Format\" rel=\"Sage.Platform.Mobile.Format\" class=\"docClass\">Sage.Platform.Mobile.Format</a></code> which has a <code>link</code> formatter.</p>\n\n<p>1. Open <code>argos-template/src/Detail/Account/Detail.js</code>.</p>\n\n<p>2. Since we are using the Format module found in the SDK add that as a dependency and reference:</p>\n\n<pre><code>define('Mobile/Template/Views/Account/Detail', [\n    'dojo/_base/declare',\n    'dojo/string',\n    'Sage/Platform/Mobile/Format',\n    'Sage/Platform/Mobile/Detail'\n], function(\n    declare,\n    string,\n    format,\n    Detail\n) {\n</code></pre>\n\n<p>3. Go down to the <code>createLayout()</code> function and to the WebAddress row object.</p>\n\n<p>4. Add the new key <code>renderer</code> and set to <code>format.link</code>:</p>\n\n<pre><code>              children: [{\n                  name: 'WebAddress',\n                  property: 'WebAddress',\n                  label: this.webAddressText,\n                  renderer: format.link\n              },{\n</code></pre>\n\n<p>5. Save and run your app and go to an Account Detail entry. The web address is now underlined and upon clicking opens the URL.</p>\n\n<p><p><img src=\"guides/v2_template_guide/detail-link.png\" alt=\"Detail Link\"></p></p>\n\n<h3>Step 2: Objects</h3>\n\n<p>The reason the row shows <code>[Object object]</code> is because the value for that SData property is actually a JSON object. In this case it's returning a 1:1 relation with an <code>owner</code> entity.</p>\n\n<p>To show the properties that we want we could either use <code>renderer</code> and provide a function that handles the object or we could use:</p>\n\n<p><code>template</code> - passes the current value as <code>$</code> to the defined <a href=\"#!/api/Simplate\" rel=\"Simplate\" class=\"docClass\">Simplate</a> and the result is displayed in the row.</p>\n\n<p>1. Back to <code>argos-template/src/Detail/Account/Detail.js</code> and down to the <code>createLayout()</code> function.</p>\n\n<p>2. For the <code>AccountManager</code> row add the key <code>tpl</code> (or <code>template</code>) with the value <code>this.userInfoNameTemplate</code>:</p>\n\n<pre><code>            },{\n                name: 'AccountManager',\n                property: 'AccountManager.UserInfo',\n                label: this.accountManagerText,\n                tpl: this.userInfoNameTemplate\n            }]\n</code></pre>\n\n<p>3. Now near the top of your code, near <code>id</code> and the localizations, add the <code>userInfoNameTemplate</code> as follows:</p>\n\n<pre><code>userInfoNameTemplate: new Simplate([\n    '{% if ($) { %}',\n        '{% if ($.LastName &amp;&amp; $.FirstName) { %}',\n            '{%: $.LastName %}, {%= $.FirstName%}',\n        '{% } else { %}',\n            '{%: $.LastName ? $.LastName : $.FirstName %}',\n        '{% } %}',\n    '{% } %}'\n]),\n</code></pre>\n\n<p>4. To go over what is happening: it uses two Simplate techniques: <code>{% executable code %}</code> and <code>{%: encoded text %}</code>. To write it out the logic: if we have an object and the object has a LastName and FirstName property join with a comma, else put whichever one we do have. In this context <code>$</code> is the object from SData and <code>$$</code> is the Detail View instance.</p>\n\n<p>5. Save and open your app to any Detail view.</p>\n\n<p><p><img src=\"guides/v2_template_guide/detail-template.png\" alt=\"Detail Template\"></p></p>\n\n<h2 id='v2_template_guide-section-7'>Create a Simple Edit View</h2>\n\n<p>Edit Views are dual purpose: Creating (POST) and Updating (PUT), the mode is determined by the navigations options.</p>\n\n<p>As with the Detail View the each Edit View is a single \"page\" that clears it's values and applies the current entry.</p>\n\n<p>Edit Views take a few extra steps to get up and running:</p>\n\n<p>1. Code the view;</p>\n\n<p>2. Register the View;</p>\n\n<p>3. Wire the Detail View to the Edit View; and</p>\n\n<p>4. Wire the List View to the Edit View.</p>\n\n<h3>Step 1: Coding an Edit View</h3>\n\n<p>1. Create an empty file named <code>Edit.js</code> into <code>argos-template\\src\\Views\\Account\\</code> and open it up.</p>\n\n<p>2. First we setup the declare and define according the AMD standard. For more information see <a href=\"#!/guides/v2_appendix_amd\">Appendix: AMD</a>.</p>\n\n<pre><code>define('Mobile/Template/Views/Account/Edit', [\n    'dojo/_base/declare',\n    'dojo/string',\n    'Sage/Platform/Mobile/Edit'\n], function(\n    declare,\n    string,\n    Edit\n) {\n    return declare('Mobile.Template.Views.Account.Edit', [Edit], {\n    });\n});\n</code></pre>\n\n<p>3. All views have a <code>titleText</code> and <code>id</code> property so let's define those first. The id standard for Edit is \"entity_edit\" meaning this view is <code>account_edit</code>.</p>\n\n<pre><code>    return declare('Mobile.Template.Views.Account.Edit', [Edit], {\n        // Localization\n        titleText: 'Account Edit',\n\n        // View Properties\n        id: 'account_edit'\n    });\n</code></pre>\n\n<p>4. Now for the data. The two Edit view properties we need are: <code>resourceKind</code> and <code>querySelect</code>. The resource kind is the endpoint of our entity and query select tells the request what fields we need.</p>\n\n<p>The reason for <code>querySelect</code> is because when we are inserting a new item it does a request for the SData defaults (<code>$template</code>) and these fields are the ones it will request and apply.</p>\n\n<ul>\n<li>resourceKind to <code>'accounts'</code></li>\n<li><p>querySelect to the exact same as your Detail querySelect: <code>AccountName</code>, <code>AccountManager/UserInfo/*</code>, <code>WebAddress</code>, <code>MainPhone</code>, and <code>Industry</code>.</p>\n\n<pre><code>      // Localization\n      titleText: 'Account Edit',\n\n      // View Properties\n      id: 'account_edit',\n      resourceKind: 'accounts',\n      querySelect: [\n          'AccountName',\n          'AccountManager/UserInfo/*',\n          'WebAddress',\n          'MainPhone',\n          'Industry'\n      ],\n</code></pre></li>\n</ul>\n\n\n<p>5. All Edit views must define a <code>createLayout()</code> function that defines the view <code>layout</code> property. The property <code>layout</code> is an array of \"field\" objects. You can also have sections like Detail and have their <code>children</code> property as \"field\" objects but normally there is no sections.</p>\n\n<pre><code>// field object example\n{\n    label: 'text shown in ui',\n    name: 'uniqueFieldId',\n    property: 'data bound property',\n    type: 'field type'\n    // additional properties related to the field type\n}\n</code></pre>\n\n<p>6. The first three are the same, but the new key: <code>type</code> designates the type of field. Examples of field types are: <code>text</code>, <code>textarea</code>, <code>date</code>, <code>decimal</code>, <code>phone</code>, <code>lookup</code> and <code>boolean</code>.</p>\n\n<p>7. Not only do field objects have the above properties but any further defined properties are passed to the field type instance. For example say we have a <code>decimal</code> field and we want the decimal places to go out to 4:</p>\n\n<pre><code>{\n    label: 'example',\n    name: 'ExampleNumber',\n    property: 'ExampleNumber',\n    type: 'decimal',\n    precision: 4\n}\n</code></pre>\n\n<p>8. Each field type has their own custom properties and you can read more about each field in the <a href=\"#!/api/Sage.Platform.Mobile.Fields._Field\" rel=\"Sage.Platform.Mobile.Fields._Field\" class=\"docClass\">API Docs</a>.</p>\n\n<p>9. Moving forward, here is the layout for our simple Edit view:</p>\n\n<pre><code>      createLayout: function() {\n          return this.layout || (this.layout = [{\n                  name: 'AccountName',\n                  property: 'AccountName',\n                  label: this.accountNameText,\n                  type: 'text'\n              },{\n                  name: 'AccountManager',\n                  property: 'AccountManager',\n                  label: this.accountManagerText,\n                  type: 'text'\n              },{\n                  name: 'Industry',\n                  property: 'Industry',\n                  label: this.industryText,\n                  type: 'text'\n              },{\n                  name: 'WebAddress',\n                  property: 'WebAddress',\n                  label: this.webAddressText,\n                  type: 'text'\n              },{\n                  name: 'MainPhone',\n                  property: 'MainPhone',\n                  label: this.mainPhoneText,\n                  type: 'text'\n              }]\n          );\n      }\n</code></pre>\n\n<p>10. As with the others go back and add the localization strings, since typically Detail and Edit mirror each other label-wise its often easy to just copy+paste from the Detail view:</p>\n\n<pre><code>titleText: 'Account Edit',\naccountNameText: 'account',\nindustryText: 'business',\naccountManagerText: 'manager',\nwebAddressText: 'web',\nmainPhoneText: 'phone',\n</code></pre>\n\n<p>11. Save your page, before it gets loaded into the App we need to register it.</p>\n\n<h3>Step 2: Register the View</h3>\n\n<p>As with all new views they must be registered in order to be loaded into the application.</p>\n\n<p>1. Open <code>argos-tempate/src/ApplicationModule.js</code> and add our new view as a dependency, making sure the reference is pulled in:</p>\n\n<pre><code>define('Mobile/Template/ApplicationModule', [\n    /* trimmed for example */\n    'Mobile/Template/Views/Account/List',\n    'Mobile/Template/Views/Account/Detail',\n    'Mobile/Template/Views/Account/Edit'\n], function(\n   /* trimmed for example */\n   AccountList,\n   AccountDetail,\n   AccountEdit\n) {\n</code></pre>\n\n<p>2. Under <code>loadViews()</code> register our view using the reference variable.</p>\n\n<pre><code>            this.registerView(new AccountList());\n            this.registerView(new AccountDetail());\n            this.registerView(new AccountEdit());\n</code></pre>\n\n<p>3. Save the file.</p>\n\n<h3>Step 3: Wire Detail to Edit</h3>\n\n<p>1. Open <code>argos-tempate/src/Views/Account/Detail.js</code> and near <code>id</code> add:</p>\n\n<pre><code>        editView: 'account_edit',\n</code></pre>\n\n<p>2. Save and open your app. Go to a detail and click the Edit button in the toolbar.</p>\n\n<p><p><img src=\"guides/v2_template_guide/edit-view.png\" alt=\"Edit View\"></p></p>\n\n<p>We'll get to sorting out the needed properties - just make sure you that the Detail and Edit Views are hooked together.</p>\n\n<h3>Step 4: Wire List to Edit</h3>\n\n<p>1. Open <code>argos-tempate/src/Views/Account/List.js</code> and near <code>detailView</code> add:</p>\n\n<pre><code>        insertView: 'account_edit',\n</code></pre>\n\n<p>2. Save and open your app. Go the Account List View and click the green plus button to add a new account.</p>\n\n<p><p><img src=\"guides/v2_template_guide/edit-view-insert.png\" alt=\"Edit View Insert\"></p></p>\n\n<h4>Additional Info</h4>\n\n<p>For more details on how the wiring for Edit Views work, including how it distinguishes Insert vs Update please see <a href=\"#!/guides/v2_beyond_the_guide_wire_detail_to_edit\">Wire Detail to Edit</a> and <a href=\"#!/guides/v2_beyond_the_guide_wire_list_to_edit\">Wire List to Edit</a>.</p>\n\n<p>Next we need to step through each of the field objects and assign the correct type while investigating any properties that field may introduce.</p>\n\n<h2 id='v2_template_guide-section-8'>Edit View Layouts</h2>\n\n<p>The Edit View currently has five text fields with only two that are actual text inputs - <code>AccountName</code> and <code>Industry</code>. The other three <code>AccountManager</code>, <code>MainPhone</code> and <code>WebAddress</code> should be changed:</p>\n\n<ul>\n<li>WebAddress to use the \"www\" keyboard input;</li>\n<li>MainPhone to the <code>phone</code> type; and</li>\n<li>AccountManager to the <code>lookup</code> type.</li>\n</ul>\n\n\n<h3>Step 1: WebAddress Input Type</h3>\n\n<p>The text field adds a property named <code>inputType</code> which directly controls the <code>&lt;input type=\"\"&gt;</code> underneath. This is useful for utilizing the latest HTML5 keyboard types.</p>\n\n<p>Currently only iOS supports the <code>url</code> keyboard providing quick keys for <code>.</code>, <code>/</code> and <code>.com</code>.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/Edit.js</code> and down to the WebAddress field.</p>\n\n<p>2. Add the <code>inputType</code> key set to <code>url</code>:</p>\n\n<pre><code>},{\n   name: 'WebAddress',\n   property: 'WebAddress',\n   label: this.webAddressText,\n   type: 'text',\n   inputType: 'url'\n},{\n</code></pre>\n\n<p>For more information and additional properties of the text field see it's API Docs.</p>\n\n<h3>Step 2: MainPhone as <code>phone</code></h3>\n\n<p>Also provided by the SDK is the <code>phone</code> type field. This field formats valid phone numbers into a more human readable format and also changes the input keyboard for iOS to the <code>tel</code> board which resembles the calling a number keyboard.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/Edit.js</code> and down to the MainPhone field.</p>\n\n<p>2. Change <code>type</code> to <code>'phone'</code></p>\n\n<pre><code>},{\n   name: 'MainPhone',\n   property: 'MainPhone',\n   label: this.mainPhoneText,\n   type: 'phone'\n}]\n</code></pre>\n\n<p>For more information and additional properties of the phone field see it's API Docs.</p>\n\n<h3>Step 3: AccountManager as <code>lookup</code></h3>\n\n<p>A <code>lookup</code> type field is one that takes the user to a different List View and let's them choose a row that is returned to the Edit view. This is how the user sets foriegn key relationships on an entity.</p>\n\n<p>1. Open <code>argos-template/src/Views/Account/Edit.js</code> and down to the AccountManager field.</p>\n\n<p>2. Change <code>type</code> to <code>'lookup'</code>.</p>\n\n<p>3. If you recall from the Detail View the textual display came from <code>AccountManager.UserInfo.FirstName</code> and <code>.LastName</code>. The Lookup Field uses the <code>textProperty</code> and <code>textTemplate</code> to do the same functionality as before. Add the two keys and set them to <code>UserInfo</code> and <code>this.userInfoNameTemplate</code>:</p>\n\n<pre><code>},{\n   name: 'AccountManager',\n   property: 'AccountManager',\n   label: this.accountManagerText,\n   textProperty: 'UserInfo',\n   textTemplate: this.userInfoNameTemplate,\n   type: 'lookup'\n},{\n</code></pre>\n\n<p>4. What the above does is take the <code>AccountManager</code> object and its <code>UserInfo</code> field (which is also an object) and passes that to the Simplate defined in <code>textTemplate</code>. Go ahead and copy the <code>userInfoNameTemplate</code> from the Detail view to the Edit view:</p>\n\n<pre><code>        // Templates\n        userInfoNameTemplate: new Simplate([\n            '{% if ($) { %}',\n                '{% if ($.LastName &amp;&amp; $.FirstName) { %}',\n                    '{%: $.LastName %}, {%= $.FirstName%}',\n                '{% } else { %}',\n                    '{%: $.LastName ? $.LastName : $.FirstName %}',\n                '{% } %}',\n            '{% } %}'\n        ]),\n</code></pre>\n\n<p>5. Save Edit.js and open your app - we are not done but let's take a look at what we have so far, make sure to edit an existing entry to see the <code>textTemplate</code> in action.</p>\n\n<p><p><img src=\"guides/v2_template_guide/edit-progress.png\" alt=\"Edit progress\"></p></p>\n\n<p>6. If you are using an iOS device to test you can checkout the URL keyboard and the tel keyboard for <code>WebAddress</code> and <code>MainPhone</code>. Note that the <code>AccountManager</code> now has a magnifying glass icon - it doesn't do anything yet but it did render the name correctly.</p>\n\n<p>7. The last piece the lookup is telling it which List View it should go to. This is controlled by the <code>view</code> property, we don't have another List view yet but will will define one soon. For now add <code>view</code> and set it to <code>user_list</code>:</p>\n\n<pre><code>},{\n   name: 'AccountManager',\n   property: 'AccountManager',\n   label: this.accountManagerText,\n   textProperty: 'UserInfo',\n   textTemplate: this.userInfoNameTemplate,\n   type: 'lookup',\n   view: 'user_list'\n},{\n</code></pre>\n\n<p>8. Save the <code>Account/Edit.js</code> file and create a new folder: <code>argos-template/src/Views/User</code> and add a <code>List.js</code> file.</p>\n\n<p>9. Create a new List view to <code>argos-template/src/Views/User/List.js</code> with the following properties:</p>\n\n<pre><code>        //Templates\n        itemTemplate: new Simplate([\n            '&lt;h3&gt;{%: $.UserInfo.LastName %}, {%: $.UserInfo.FirstName %}&lt;/h3&gt;',\n            '&lt;h4&gt;{%: $.UserInfo.Title %}&lt;/h4&gt;'\n        ]),\n\n        //Localization\n        titleText: 'Users',\n\n        //View Properties\n        id: 'user_list',\n        queryOrderBy: 'UserInfo.LastName asc, UserInfo.FirstName asc',\n        querySelect: [\n            'UserInfo/FirstName',\n            'UserInfo/LastName',\n            'UserInfo/Title',\n            'UserInfo/UserName'\n        ],\n        resourceKind: 'users'\n</code></pre>\n\n<p>10. Don't forget to register the view. Save and open your app. Try either adding a new account or editing an existing one - use the lookup to select a different Account Manager from the User endpoint.</p>\n\n<p>That's it! You've completed the Argos-Template Guide. If you want to download a previously finished copy of all of the above you may download the zipped archive here: <a href=\"guides/v2_template_guide/argos-template_guide-complete.zip\">Completed Template Guide</a>.</p>\n","title":"Argos-Template Guide"});