Ext.data.JsonP.v2_beyond_the_guide_defining_list_actions({"guide":"<h1>Defining List Actions</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/v2_beyond_the_guide_defining_list_actions-section-1'>Layout</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_defining_list_actions-section-2'>Action Objects</a></li>\n<li><a href='#!/guide/v2_beyond_the_guide_defining_list_actions-section-3'>Enabling List Actions</a></li>\n</ol>\n</div>\n\n<p>List Views may have what is called List Actions. Basically it treats every row as an actionable item so that tapping that row's icon displays a context-sensitive action ribbon below that row. As an example you may have a List view of Service Tickets, you could then define a list action bar with actions like \"Close Ticket\", \"Delete Ticket\" and \"Call Customer\".</p>\n\n<p>The context-sensitive portion refers to the fact that the actions will have access to that rows data, most importantly the $key of that row.</p>\n\n<h2 id='v2_beyond_the_guide_defining_list_actions-section-1'>Layout</h2>\n\n<p>Defining List Actions is very similar to toolbars and layouts for views, it takes place within the <code>createActionLayout()</code> function and it returns and defines the <code>this.actions</code> array:</p>\n\n<pre><code>createActionLayout: function() {\n    return this.actions || (this.actions = [ /* action objects /* ]\n    );\n}\n</code></pre>\n\n<h2 id='v2_beyond_the_guide_defining_list_actions-section-2'>Action Objects</h2>\n\n<p>The action objects that describe each action button are, again, similar to toolbar objects:</p>\n\n<pre><code>{\n    id: 'edit',\n    icon: 'content/images/icons/edit_24.png',\n    label: this.editActionText,\n    enabled: this.isEditEnabled,\n    action: 'navigateToEditView',\n    /* fn: this.navigateToEditView or you can define a fn instead of action */\n}\n</code></pre>\n\n<ul>\n<li><code>id</code> - the unique identify for the action.</li>\n<li><code>icon</code> - path to the icon to be used.</li>\n<li><code>label</code> - the text shown below the icon.</li>\n<li><code>enabled</code> - determines the enabled/disabled state. Can be <code>true/false</code> or a function that returns <code>true/false</code>.</li>\n<li><code>fn</code> - Function to be called. It will be passed <code>(action, selection)</code> where:\n\n<ul>\n<li><code>action</code> is the action object defined</li>\n<li><code>selection</code> is the selected object and you will mainly want <code>selection.data</code> for the row context.</li>\n</ul>\n</li>\n<li><code>action</code> - String name of the views function to be called on click. This is a shortcut instead of <code>fn: this.functionName</code>. See <code>fn</code> for the parameters passed.</li>\n<li><code>template</code> - Simplate to be used instead of the List views <code>listActionItemTemplate</code> property.</li>\n<li><code>security</code> - String or function. If defined it will be passed to the <code><a href=\"#!/api/Sage.Platform.Mobile.Application-method-hasAccessTo\" rel=\"Sage.Platform.Mobile.Application-method-hasAccessTo\" class=\"docClass\">App.hasAccessTo</a>()</code> where that string will be compared to the array of secured actions. If it is not found in that list it will return false and the action object will be disabled (unless the array of actions is empty, in which it returns true for all <code>hasAccessTo</code> requests).</li>\n<li><code>scope</code> - object, if defined and has <code>fn</code> defined the <code>fn</code> function will be invoked with the <code>scope</code> as the <code>this</code> property of the function. If not defined it will use the current view as the scope.</li>\n</ul>\n\n\n<h2 id='v2_beyond_the_guide_defining_list_actions-section-3'>Enabling List Actions</h2>\n\n<p>Even with <code>createActionLayout</code> defined there are still two flags to be on the List view in order to enable action items to be display:</p>\n\n<ul>\n<li><code>allowSelection</code> to <code>true</code>; and</li>\n<li><code>enableActions</code> to <code>true</code>.</li>\n</ul>\n\n\n<p>The first flag lets the view know that when a row is clicked it is making a selection and to keep track of any selections made. The second flag signifies that this view has actions and it adjusts various CSS classes to show the icon next to each row.</p>\n\n<p>These two flags can also be sent via navigation options to the list views to disable/hide the actions. This is used in all lookup fields because when making a selection it should show these actions.</p>\n","title":"List Actions"});